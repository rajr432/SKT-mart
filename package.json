{
  "name": "skt-mart",
  "version": "0.1.0",
  "private": true,
  "description": "SKT Mart - Multi-vendor e-commerce platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:api": "npm --workspace @skt/api run dev",
    "dev:web": "npm --workspace @skt/web run dev",
    "build": "npm --workspace @skt/api run build && npm --workspace @skt/web run build",
    "lint": "npm --workspace @skt/api run lint && npm --workspace @skt/web run lint",
    "typecheck": "npm --workspace @skt/api run typecheck && npm --workspace @skt/web run typecheck",
    "db:migrate": "npm --workspace @skt/api run db:migrate",
    "db:seed": "npm --workspace @skt/api run db:seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
